#Installing the neccessary packages
library(tidyverse)
library(janitor)
library(ggplot2)

#Loading and cleansing data sets
continents <- read.csv("continents-according-to-our-world-in-data.csv") %>% clean_names()
gdp <- read.csv("gdp-per-capita-worldbank.csv") %>% clean_names()

names (gdp)
head(gdp)

# Combining data sets
comb_gdp <- gdp %>%
  select(entity, code, year,gdp_per_capita_ppp_constant_2017_international) %>%
  left_join(continents %>% select(code, continent),by = "code")

# Computation of GDP per capita growth
comb_growth <- comb_gdp %>%
  group_by(entity) %>%
  arrange(year) %>%
  mutate(growth_rate = (gdp_per_capita_ppp_constant_2017_international -lag(gdp_per_capita_ppp_constant_2017_international)) /lag(gdp_per_capita_ppp_constant_2017_international) * 100
  ) %>%
  ungroup()

# GDP growth (continents)
cont_growth <- comb_growth %>%
  group_by(continent, year) %>%
  summarise(mean_growth = mean(growth_rate, na.rm = TRUE)) %>%
  ungroup()

#Plotting graphs only involving given data sets
ggplot(cont_growth, aes(x = year, y = mean_growth, color = continent)) +
  geom_line(linewidth = 0.7, alpha = 0.85) + geom_point(size = 1.3, alpha = 0.9) +
  labs(title = "GDP Per Capita Growth Across Continents", x = "Year", y = "Average Growth (%)",color = "Continent") +
  theme_minimal(base_size = 14) +
  theme(legend.position = "bottom", panel.grid.minor = element_blank()
  )

#Combining everything with new data set

income <- read.csv("CLASS_2025_10_07(in).csv") %>% clean_names()

comb_gdp2 <- comb_gdp %>%
  left_join(income %>% select(code, income_group, lending_category), by = "code")
comb_gdp2 <- comb_gdp2 %>%
  mutate(ldc = if_else(income_group == "Low income", "LDC Countries", "Non-LDC Countries"))

comb_growth2 <- comb_gdp2 %>%
  group_by(entity) %>%
  arrange(year) %>%
  mutate(growth_rate =
           (gdp_per_capita_ppp_constant_2017_international - lag(gdp_per_capita_ppp_constant_2017_international)) / lag(gdp_per_capita_ppp_constant_2017_international) * 100) %>%
  ungroup()

# Continent-level growth (Inc 4th CSV)
cont_growth2 <- comb_growth2 %>%
  group_by(continent, year) %>%
  summarise(mean_growth = mean(growth_rate, na.rm = TRUE)) %>%
  ungroup()

# LDC-only growth
ldc_growth <- comb_growth2 %>%
  filter(ldc == "LDC Countries") %>%
  group_by(continent, year) %>%
  summarise(mean_growth = mean(growth_rate, na.rm = TRUE)) %>%
  ungroup()

# Gap to 7% target
ldc_gap <- ldc_growth %>%
  mutate(gap_to_target = mean_growth - 7)

#Plot 1
ldc_growth <- comb_growth2 %>%
filter(ldc == "LDC Countries") %>%
group_by(continent, year) %>%
summarise(mean_growth = mean(growth_rate, na.rm = TRUE)) %>%
ungroup()
ldc_gap <- ldc_growth %>%
mutate(gap_to_target = mean_growth - 7)
ggplot(ldc_gap, aes(x = year, y = gap_to_target, fill = continent)) + geom_col(alpha = 0.9) +
geom_hline(yintercept = 0, color = "black", linetype = "dashed", linewidth = 1) +
labs( title = "LDC Countries- Contribution of Each Continent to Gap From Target", subtitle =
"Stacked bars show how continents push total LDC growth above or below 7%", x = "Year", y
= "Gap to Target (percentage points)", fill = "Continent" ) +
theme_minimal(base_size = 14) +
theme( legend.position = "bottom", panel.grid.minor = element_blank() )


#Plot 2
income_stats <- comb_growth2 %>%
  group_by(income_group) %>%
  summarise(avg_growth = mean(growth_rate, na.rm = TRUE), volatility = sd(growth_rate, na.rm = TRUE), .groups = "drop" )

scale_factor <- max(income_stats$avg_growth, na.rm = TRUE) /
  max(income_stats$volatility, na.rm = TRUE)

ggplot(income_stats, aes(x = income_group)) +
  geom_col(aes(y = avg_growth, fill = income_group), alpha = 0.8) +
  geom_line(aes(y = volatility * scale_factor, group = 1), color = "black", linewidth = 0.7) +
  geom_point(aes(y = volatility * scale_factor),color = "black", size = 3) +
  scale_y_continuous( name = "Average GDP Per Capita Growth (%)", sec.axis = sec_axis(~ . / scale_factor, name = "Volatility (Std Dev)")) +
  labs( title = "Growth vs Volatility Across Income Groups", subtitle = "Bars = average growth | Line = volatility (dual axis)", x = "Income Group", fill = "Income Group") +
  theme_minimal(base_size = 14) +
  theme(legend.position = "none")

#PLOT 4
inc_group <- comb_growth2 %>%
  filter(!is.na(income_group), !is.na(continent)) %>%
  group_by(income_group, year) %>%
  summarise(mean_growth = mean(growth_rate, na.rm = TRUE), .groups = "drop")

ggplot(inc_group, aes(x = year, y = mean_growth)) +
  geom_line(linewidth = 0.7, color = "black") +
  geom_point(size = 0.7, color = "black", alpha = 0.85) +
  geom_hline(yintercept = 7, linetype = "dashed", color = "red", linewidth = 0.7) +
  facet_wrap(~ income_group, ncol = 2, scales = "free_y") +
  labs(title = "GDP Per Capita Growth by Income Classification", subtitle = "Red dashed line marks the 7% SDG target for Low income countries", x = "Year", y = "Average Growth (%)") +
  theme_minimal(base_size = 14) 



